namespace Subjects {
  export class Subject {
    teacher: Teacher;

    // Setter method to set the teacher attribute
    setTeacher(teacher: Teacher): void {
      this.teacher = teacher;
    }
  }
}
